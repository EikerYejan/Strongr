import {StyleSheet} from "react-native"

export const tabNavigationButtonStyles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignContent: "center",
    alignItems: "center"
  }
})
